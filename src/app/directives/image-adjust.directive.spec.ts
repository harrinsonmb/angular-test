import { ImageAdjustDirective } from './image-adjust.directive';

describe('ImageAdjustDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageAdjustDirective();
    expect(directive).toBeTruthy();
  });
});
